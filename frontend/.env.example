# Copy this file to .env and fill in the values for local development.
# .env is gitignored and should never be committed.
#
# When running via Docker Compose, BACKEND_URL is set automatically
# via the environment section in docker-compose.yml — no .env needed.

# URL of the FastAPI backend that Vite's dev-server proxy forwards API requests to.
BACKEND_URL=http://localhost:8000
